{
  "name": "compound-rsvp",
  "version": "1.0.0",
  "private": true,
  "description": "Compound x muShanghai - From the Future RSVP & blood-draw slot booking",
  "scripts": {
    "dev": "wrangler pages dev",
    "db:local": "wrangler d1 execute compound-rsvp-db --local --file=./schema.sql",
    "db:remote": "wrangler d1 execute compound-rsvp-db --remote --file=./schema.sql",
    "deploy": "wrangler pages deploy"
  },
  "devDependencies": {
    "wrangler": "^4.93.0"
  }
}
